:root {
    --primary: #6ac5ff;
    --secondary: #ff6b6b;
    --bg: #0f172a;
    --text: #f8fafc;
    --glass: rgba(15, 23, 42, 0.8);
}

[data-theme="light"] {
    --bg: #f8fafc;
    --text: #0f172a;
    --glass: rgba(248, 250, 252, 0.8);
}

body {
    font-family: 'Geist', 'Inter', sans-serif;
    background: var(--bg);
    color: var(--text);
    min-height: 100vh;
}

.glass-header {
    background: var(--glass);
    backdrop-filter: blur(10px);
    padding: 1.5rem;
    position: fixed;
    width: 100%;
    z-index: 100;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid rgba(255,255,255,0.1);
}

.logo {
    font-size: 1.8rem;
    font-weight: 800;
}

.logo-code {
    color: var(--primary);
}

.cta-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
}

.cta-card {
    background: var(--glass);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 12px;
    padding: 2rem;
    transition: all 0.3s ease;
}

.cta-card:hover {
    transform: translateY(-5px);
    border-color: var(--primary);
    box-shadow: 0 10px 20px rgba(106, 197, 255, 0.1);
}
